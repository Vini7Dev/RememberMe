// Storage collections path
const STORAGE_HOOT = '@RememberMe';
const COLLECTION_USERNAME = `${STORAGE_HOOT}:name`;
const COLLECTION_TASKS = `${STORAGE_HOOT}:tasks`;

export {
  STORAGE_HOOT,
  COLLECTION_USERNAME,
  COLLECTION_TASKS,
};
